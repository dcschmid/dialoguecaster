[tool.black]
line-length = 120

[tool.ruff]
line-length = 120

[tool.ruff.lint]
extend-select = ["E", "W", "F"]

[tool.ruff.lint.per-file-ignores]
"tests/*.py" = ["E402"]  # Module imports not at top (needed for sys.path manipulation)
